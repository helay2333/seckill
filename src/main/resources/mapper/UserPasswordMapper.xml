<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserPasswordMapper">
    <select id="selectByUserId" resultType="com.example.demo.entity.UserPassword">
        select * from user_password where user_id=#{id}
    </select>

    <insert id="insertPassword">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Sun Nov 18 19:13:00 CST 2018.
        -->
        insert into user_password
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="encrptPassword != null">
                encrpt_password,
            </if>
            <if test="userId != null">
                user_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="encrptPassword != null">
                #{encrptPassword},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
        </trim>
    </insert>

</mapper>
